package Algorithm.Amazon;

/**
 * Created by i843719 on 11/26/14.
 */
public class NextGreaterElement {
    // [4, 5, 3, 23] --->[5, 23, 23, -1]
    // brute-force method using O(n^2) definitely can solve this question
    // another method is using a stack to record its comparatively small digits
    // then compare the small ones with the next small
    // question is how to know the index
    // our solution is build an additional data structure, which contains the index
    // or just use a index, 
    public int[] convert(int[] data){
        int[] res = new int[data.length];





        return res;
    }
}
